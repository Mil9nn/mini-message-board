<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/styles.css" rel="stylesheet">
    <title><%= title %></title>
</head>

<body>
    <nav>
        <h1><span>Mini Message Board</span> <span class="highlight">Using NodeJs</span></h1>
        <div class="nav-links">
            <a class="active" href="/">Messages</a>
            <a href="/new">Write a message</a>
        </div>
    </nav>
    <ul class="message-list">
        <% messages.forEach(msg=> { %>
            <li class="message-item">
                <strong>
                    <%= msg.user %>:
                </strong>
                <%= msg.text %><br>
                    <small>
                        <%= msg.added.toLocaleString() %>
                    </small>
            </li>
            <% }) %>
    </ul>
</body>

</html>